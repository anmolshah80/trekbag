import Logo from '@/components/Logo';
import Counter from '@/components/Counter';

const Header = () => {
  return (
    <header>
      <Logo />
      <Counter />
    </header>
  );
};

export default Header;
